# 📱 手机远程 SSH 连接 Mac 电脑完整教程

> 使用 Tailscale 实现异地组网，无需公网 IP，安全可靠

---

## 📋 目录

- [使用场景](#使用场景)
- [原理简介](#原理简介)
- [电脑端配置](#电脑端配置)
- [手机端配置](#手机端配置)
- [SSH 连接](#ssh-连接)
- [进阶：配置 Exit Node](#进阶配置-exit-node)
- [常见问题](#常见问题)

---

## 使用场景

### 场景一：手机远程 AI 编程 💻

> 当手机和电脑不在同一网络时，想在手机上使用 Kimi CLI 等 AI 编程工具

**实际案例：**
- 你在咖啡馆用手机，家里电脑开着
- 通过 SSH 连接到 Mac 终端
- 直接使用 Kimi CLI 进行代码审查、生成、重构

```bash
# 手机 SSH 连接后
kimi "帮我优化这段代码"
kimi "解释一下这个报错"
kimi "生成一个 Python 脚本"
```

**优势：**
- 不需要在手机上安装复杂的开发环境
- 利用电脑的性能和完整工具链
- AI 编程助手随时可用

---

### 场景二：远程重启 OpenClaw 网关 🤖

> OpenClaw Gateway 意外关闭，但电脑不在身边

**实际案例：**
- OpenClaw Gateway 进程崩溃，Discord Bot 下线
- 你在外面，无法物理接触电脑
- 通过手机 SSH 远程重启服务

```bash
# 连接到电脑后
pm2 status                    # 查看网关状态
pm2 restart openclaw-gateway  # 重启网关
pm2 logs openclaw-gateway     # 查看日志
```

**解决的痛点：**
- 不再担心服务宕机后必须回家处理
- 随时随地维护你的 AI 助手
- 紧急情况下快速恢复服务

---

### 场景三：异地文件管理 📂

> 需要访问电脑上的文件，但没带电脑

**实际案例：**
- 重要文档存在家里电脑上
- 需要通过 SFTP 或命令行查找、传输文件
- 甚至可以用 `vim` 远程编辑配置文件

```bash
# 远程查看文件
ls ~/Documents/important/
cat ~/.config/app/config.yml

# 配合 SCP 从手机传输文件
scp file.txt user@100.x.x.x:~/Downloads/
```

---

### 场景四：监控和运维 📊

> 随时查看电脑运行状态

```bash
# 查看系统状态
top                    # CPU/内存使用
df -h                  # 磁盘空间
uptime                 # 运行时间

# 查看特定服务
curl localhost:3000/health  # 检查 API 健康状态
tail -f /var/log/system.log # 实时日志
```

---

## 原理简介

Tailscale 基于 WireGuard 协议，创建一个虚拟私有网络（VPN），让你的设备即使在不同网络下也能像在同一局域网内通信。

**核心优势：**
- ✅ 无需公网 IP，无需端口转发
- ✅ 端到端加密，安全可靠
- ✅ P2P 直连优先，延迟低
- ✅ 支持 iOS、Android、macOS、Windows、Linux

```
手机 (4G/5G) ───► Tailscale 网络 ───► 家里电脑
      │                                │
      └────────── WireGuard 隧道 ──────┘
              (端到端加密)
```

---

## 电脑端配置

### 1. 安装 Tailscale

```bash
# macOS (使用 Homebrew)
brew install tailscale

# 或使用官方安装包
# https://tailscale.com/download
```

### 2. 启动并登录

```bash
# 启动系统服务（需要密码）
sudo tailscaled install-system-daemon

# 登录（会弹出浏览器认证）
tailscale up

# 查看分配到的 IP
tailscale ip -4
# 输出示例：100.x.x.x
```

### 3. 开启 macOS 远程登录（SSH）

**方法 A - 图形界面：**
```
系统设置 → 通用 → 共享 → 远程登录 → 开启
```

**方法 B - 终端：**
```bash
sudo systemsetup -setremotelogin on
```

**验证 SSH 是否工作：**
```bash
ssh localhost
# 输入密码后能登录即成功
```

### 4. 设置开机自启（可选）

```bash
# 安装 PM2（进程管理器）
brew install pm2

# 创建启动脚本
cat > ~/tailscale-keeper.sh << 'EOF'
#!/bin/bash
while true; do
    if ! tailscale status &>/dev/null; then
        tailscale up --accept-routes
    fi
    sleep 60
done
EOF
chmod +x ~/tailscale-keeper.sh

# 使用 PM2 管理
pm2 start ~/tailscale-keeper.sh --name tailscale-keeper
pm2 save

# 设置 PM2 开机自启
pm2 startup
```

---

## 手机端配置

### 1. 安装 Tailscale App

- **iOS**: App Store 搜索 "Tailscale"
- **Android**: Google Play 或官网下载

### 2. 登录

使用**与电脑相同的账号**登录（GitHub/Google/微软账号）

### 3. 验证连接

登录后，你应该能在 App 中看到：
- 你的电脑名称
- 电脑分配的 IP（如 `100.x.x.x`）
- 连接状态显示为 "Connected"

---

## SSH 连接

### 使用 iOS SSH 客户端

推荐 App：
- **Termius**（免费，推荐）
- **Prompt 2**（付费，功能强大）
- **Blink Shell**（付费，专业级）

**连接配置：**

| 配置项 | 值 |
|--------|-----|
| 主机/地址 | `100.x.x.x`（你的电脑 Tailscale IP） |
| 端口 | `22` |
| 用户名 | `你的 macOS 用户名` |
| 密码 | `你的 macOS 登录密码` |

或使用 **SSH 密钥**（更安全）：

```bash
# 在电脑上生成密钥（如尚未生成）
ssh-keygen -t ed25519

# 查看公钥
cat ~/.ssh/id_ed25519.pub
```

将私钥导入手机 SSH App，即可免密码登录。

### 测试连接

连接成功后，试试这些命令：
```bash
whoami          # 显示用户名
hostname        # 显示电脑名
pwd             # 当前目录
ls -la          # 列出文件
```

---

## 进阶：配置 Exit Node

如果你想让手机所有流量都经过电脑（实现"翻墙"或统一出口 IP），可以配置 Exit Node。

### 电脑端配置

```bash
# 开启 Exit Node 功能
sudo tailscale up --advertise-exit-node --accept-routes
```

### 管理后台授权

1. 访问 https://login.tailscale.com/admin/machines
2. 找到你的电脑，点击菜单 → "Edit route settings..."
3. ✅ 勾选 "Use as exit node" → Save

### 手机端启用

1. 打开 Tailscale App
2. 点击你的电脑设备
3. 开启 "Use as exit node"
4. 验证：访问 ip.sb，应显示电脑的公网 IP

---

## 常见问题

### Q1: 手机连接后显示 "offline"

**原因：** iOS 后台限制，锁屏后 VPN 可能被暂停

**解决：** 重新打开 Tailscale App 即可

### Q2: iOS 无法同时运行 Tailscale 和其他 VPN

**原因：** iOS 限制只能有一个 VPN 隧道

**解决：**
- 使用 Exit Node 功能（让 Tailscale 兼任翻墙）
- 或按需切换 VPN

### Q3: 连接成功但 SSH 失败

**排查步骤：**
```bash
# 在电脑上检查 SSH 服务
sudo lsof -i :22

# 检查 Tailscale 状态
tailscale status

# 检查防火墙
sudo /usr/libexec/ApplicationFirewall/socketfilterfw --getglobalstate
```

### Q4: 电脑重启后 Tailscale 没自动连接

**解决：**
```bash
# 手动启动
tailscale up

# 或使用 PM2 自动恢复
pm2 resurrect
```

### Q5: Tailscale IP 会变吗？

**不会。** 只要设备不被删除，分配的 `100.x.x.x` IP 是固定的。

---

## 📝 附录

### 常用命令

```bash
# 查看状态
tailscale status
tailscale status --json

# 查看本机 IP
tailscale ip -4

# 停止/启动
tailscale down
tailscale up

# 查看网络路径（判断是否直连）
tailscale status | grep -E "direct|relay"
```

### 安全建议

1. **启用设备审批**：在 Tailscale 管理后台开启 "Device approval"，新设备需要管理员批准
2. **使用 ACL**：限制设备间的访问权限
3. **定期轮换密钥**：Tailscale 会自动处理
4. **禁用不用的设备**：及时在后台移除丢失或不再使用的设备

---

## 参考链接

- [Tailscale 官网](https://tailscale.com)
- [WireGuard 协议](https://www.wireguard.com/)
- [macOS 远程登录官方文档](https://support.apple.com/guide/mac-help/allow-a-remote-computer-to-access-your-mac-mchlp1066/mac)

---

**作者：** @ruska  
**创建时间：** 2026-02-20  
**适用平台：** macOS + iOS/Android
