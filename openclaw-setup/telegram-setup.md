# 📱 OpenClaw 配置 Telegram Bot 完整教程

> 将 OpenClaw 接入 Telegram，随时随地与 AI 助手对话

---

## 📋 目录

- [为什么要接入 Telegram](#为什么要接入-telegram)
- [原理简介](#原理简介)
- [前置要求](#前置要求)
- [第一步：创建 Telegram Bot](#第一步创建-telegram-bot)
- [第二步：配置 OpenClaw](#第二步配置-openclaw)
- [第三步：启动 Gateway](#第三步启动-gateway)
- [第四步：配对与授权](#第四步配对与授权)
- [第五步：测试与使用](#第五步测试与使用)
- [群组配置](#群组配置)
- [常见问题](#常见问题)
- [安全设置](#安全设置)

---

## 为什么要接入 Telegram

### 使用场景 💡

| 场景 | 描述 |
|------|------|
| **移动办公** | 手机上随时与 AI 对话，无需打开电脑 |
| **通知提醒** | AI 主动推送重要信息到手机 |
| **团队协作** | 群组中共享 AI 助手，多人使用 |
| **多设备同步** | 手机、平板、电脑都能访问同一个对话 |

### 优势对比

| 方式 | 便利性 | 实时性 | 多设备 |
|------|:------:|:------:|:------:|
| WebChat | ⭐⭐⭐ | ⭐⭐ | ❌ |
| Discord | ⭐⭐⭐ | ⭐⭐⭐ | ✅ |
| **Telegram** | ⭐⭐⭐⭐⭐ | ⭐⭐⭐⭐⭐ | ✅ |
| iMessage | ⭐⭐⭐⭐ | ⭐⭐⭐⭐ | ❌ |

---

## 原理简介

### 架构图

```
┌─────────────────┐
│   你的手机       │
│  Telegram App   │
└────────┬────────┘
         │
         ▼
┌─────────────────┐      ┌──────────────────┐      ┌─────────────────┐
│  Telegram 服务器 │◄────►│  OpenClaw        │◄────►│  AI 模型        │
│  (Bot API)      │      │  (Gateway)       │      │  (Claude/GPT等) │
└─────────────────┘      └──────────────────┘      └─────────────────┘
                                │
                                ▼
                         ┌──────────────┐
                         │  ~/.openclaw │
                         │  配置和记忆   │
                         └──────────────┘
```

### 工作流程

1. 你在 Telegram 发送消息给 Bot
2. Telegram 服务器转发到 OpenClaw Gateway
3. OpenClaw 处理消息，调用 AI 模型
4. 模型返回结果给 OpenClaw
5. OpenClaw 将回复发送到 Telegram

---

## 前置要求

### 必须已完成 ✅

- [ ] OpenClaw 已安装（`npm install -g openclaw`）
- [ ] OpenClaw Gateway 可以正常运行
- [ ] 有 Telegram 账号（手机 App 或桌面版）

### 需要准备 📝

| 项目 | 说明 |
|------|------|
| Telegram 账号 | 个人账号，用于创建 Bot 和测试 |
| 手机或电脑 | 安装 Telegram App |

---

## 第一步：创建 Telegram Bot 🤖

### 1.1 打开 BotFather

在 Telegram 中搜索 `@BotFather`（注意认准官方账号，有蓝色认证勾）：

```
1. 点击搜索框
2. 输入 "BotFather"
3. 选择 @BotFather（有 ✓ 标记）
4. 点击 "Start" 或发送 /start
```

### 1.2 创建新 Bot

发送命令创建 Bot：

```
/newbot
```

BotFather 会要求你提供：

1. **Bot 名称**（显示名称，可改）
   ```
   示例：我的AI助手
   ```

2. **Bot 用户名**（唯一标识，不可改，必须以 bot 结尾）
   ```
   示例：my_ai_helper_bot
   ```

⚠️ **注意**：如果用户名已被占用，需要换一个新的。

### 1.3 保存 Token

创建成功后，BotFather 会发送：

```
Done! Congratulations on your new bot. You will find it at t.me/my_ai_helper_bot.

Use this token to access the HTTP API:
123456789:ABCdefGHIjklMNOpqrsTUVwxyz123456789
```

**重要**：
- ✅ 复制并保存 `123456789:ABCdef...` 这串字符（这就是 Bot Token）
- 🔒 **不要泄露给任何人**，这是控制 Bot 的密钥
- 📝 建议保存在密码管理器或安全的地方

---

## 第二步：配置 OpenClaw ⚙️

### 2.1 编辑配置文件

打开 OpenClaw 配置文件：

```bash
# 使用你喜欢的编辑器
nano ~/.openclaw/openclaw.json
# 或
vim ~/.openclaw/openclaw.json
# 或 VS Code
code ~/.openclaw/openclaw.json
```

### 2.2 添加 Telegram 配置

在 `channels` 部分添加 Telegram 配置：

```json
{
  "channels": {
    "telegram": {
      "enabled": true,
      "botToken": "你的BotToken",
      "dmPolicy": "pairing",
      "allowFrom": [],
      "groupPolicy": "allowlist",
      "groups": {
        "*": {
          "requireMention": true
        }
      }
    }
  }
}
```

**配置说明**：

| 配置项 | 说明 | 推荐值 |
|--------|------|--------|
| `enabled` | 是否启用 Telegram | `true` |
| `botToken` | BotFather 给的 Token | 你的 Token |
| `dmPolicy` | 私聊访问控制 | `"pairing"`（安全） |
| `allowFrom` | 允许的用户 ID 列表 | `[]`（空数组，先配对） |
| `groupPolicy` | 群组访问控制 | `"allowlist"` |
| `requireMention` | 群组中是否需要 @Bot | `true` |

### 2.3 完整配置示例

```json
{
  "agents": {
    "defaults": {
      "model": {
        "primary": "openrouter/stepfun/step-3.5-flash:free"
      },
      "workspace": "/Users/你的用户名/.openclaw/workspace"
    }
  },
  "channels": {
    "telegram": {
      "enabled": true,
      "botToken": "123456789:ABCdefGHIjklMNOpqrsTUVwxyz",
      "dmPolicy": "pairing",
      "allowFrom": [],
      "groupPolicy": "allowlist",
      "groups": {
        "*": {
          "requireMention": true
        }
      },
      "streamMode": "partial"
    }
  },
  "gateway": {
    "port": 18789,
    "auth": {
      "mode": "token",
      "token": "你的GatewayToken"
    }
  }
}
```

### 2.4 保存配置

保存文件后，验证配置格式是否正确：

```bash
# 检查 JSON 格式
cat ~/.openclaw/openclaw.json | python3 -m json.tool > /dev/null && echo "✅ JSON 格式正确" || echo "❌ JSON 格式错误"
```

---

## 第三步：启动 Gateway 🚀

### 3.1 启动 OpenClaw Gateway

```bash
# 直接启动
openclaw gateway --verbose

# 或使用 pm2 管理（推荐）
pm2 start openclaw-gateway

# 如果是第一次配置 pm2
cd /路径/到/openclaw
pm2 start openclaw.mjs --name openclaw-gateway -- gateway --verbose
```

### 3.2 检查启动状态

```bash
# 查看进程状态
pm2 status

# 预期输出
┌────┬─────────────────────┬─────────────┬─────────┬─────────┬──────────┬────────┬──────┬───────────┐
│ id │ name                │ namespace   │ version │ mode    │ pid      │ uptime │ ↺    │ status    │
├────┼─────────────────────┼─────────────┼─────────┼─────────┼──────────┼────────┼──────┼───────────┤
│ 1  │ openclaw-gateway    │ default     │ 2026.2… │ fork    │ 12345    │ 10s    │ 0    │ online    │
└────┴─────────────────────┴─────────────┴─────────┴─────────┴──────────┴────────┴──────┴───────────┘
```

### 3.3 检查日志

```bash
# 查看实时日志
pm2 logs openclaw-gateway

# 预期看到 Telegram 启动信息
[telegram] [default] starting provider (@你的Bot用户名)
[telegram] autoSelectFamily=true
```

如果看到错误信息，检查：
- Bot Token 是否正确
- 网络连接是否正常
- 端口 18789 是否被占用

---

## 第四步：配对与授权 🔐

### 4.1 在 Telegram 中发送消息

1. 在 Telegram 搜索你的 Bot 用户名（如 `@my_ai_helper_bot`）
2. 点击 "Start" 或发送任意消息（如 "hello"）
3. Bot 会回复一个**配对码**（如 `MKXNWRSF`）

### 4.2 查看待配对请求

在电脑终端执行：

```bash
# 查看待配对的请求
openclaw pairing list telegram

# 预期输出
Pending pairings for telegram:
  Code: MKXNWRSF
  From: 123456789 (User Name)
  Time: 2026-02-20 10:30:00
```

### 4.3 批准配对

```bash
# 批准配对（替换为实际配对码）
openclaw pairing approve telegram MKXNWRSF

# 预期输出
Approved telegram sender 123456789.
```

### 4.4 添加到白名单（可选但推荐）

获取你的 Telegram 用户 ID：

```bash
# 查看日志中的用户 ID
pm2 logs openclaw-gateway | grep "from.id"

# 或在批准配对时已经显示
# Approved telegram sender 123456789.
```

编辑配置文件，将用户 ID 添加到 `allowFrom`：

```json
{
  "channels": {
    "telegram": {
      "allowFrom": ["123456789"]
    }
  }
}
```

这样以后就不需要每次都配对了。

---

## 第五步：测试与使用 ✅

### 5.1 发送测试消息

在 Telegram 中发送：

```
你好！
```

预期回复：
- Bot 会回复消息
- 回复来自你配置的 AI 模型

### 5.2 常用命令测试

| 命令 | 作用 |
|------|------|
| `/help` | 查看帮助 |
| `/status` | 查看当前状态 |
| `/model` | 查看/切换模型 |
| `/reset` | 重置会话 |

### 5.3 发送图片/文件

Telegram Bot 支持：
- ✅ 接收图片（AI 可以分析图片内容）
- ✅ 接收语音（需要配置语音识别）
- ✅ 发送文件给 AI 分析

---

## 群组配置 👥

### 将 Bot 添加到群组

1. 在 Telegram 群组中，点击群组名称
2. 选择 "Add Members"
3. 搜索你的 Bot 用户名（如 `@my_ai_helper_bot`）
4. 添加为成员

### 配置群组权限

编辑 `openclaw.json`：

```json
{
  "channels": {
    "telegram": {
      "groups": {
        "-1001234567890": {
          "slug": "我的群组",
          "requireMention": true,
          "groupPolicy": "allowlist",
          "allowFrom": ["123456789"]
        }
      }
    }
  }
}
```

**获取群组 ID**：
- 方法1：转发群组消息到 `@userinfobot`
- 方法2：查看 OpenClaw 日志中的 `chat.id`

### 群组使用方式

- 如果 `requireMention: true`，需要 `@Bot名` 才能触发
- 如果 `requireMention: false`，Bot 会监听所有消息

---

## 常见问题 ❓

### Q1: Bot 不回复消息

**排查步骤**：

1. 检查 Gateway 是否运行
   ```bash
   pm2 status
   ```

2. 检查日志是否有错误
   ```bash
   pm2 logs openclaw-gateway
   ```

3. 确认配对已批准
   ```bash
   openclaw pairing list telegram
   ```

4. 检查网络连接
   ```bash
   curl https://api.telegram.org/bot<你的Token>/getMe
   ```

### Q2: 提示 "未授权"

**原因**：用户不在 `allowFrom` 列表中，且未完成配对

**解决**：
- 完成配对流程（见第四步）
- 或将用户 ID 添加到 `allowFrom`

### Q3: 群组中 Bot 不响应

**排查**：
1. 确认 Bot 已在群组中
2. 检查是否需要 `@Bot名`
3. 查看群组 ID 是否配置正确
4. 检查 `groupPolicy` 设置

### Q4: 如何修改 Bot 信息

在 Telegram 中向 `@BotFather` 发送：

```
/setname - 修改 Bot 名称
/setdescription - 修改描述
/setabouttext - 修改关于文本
/setuserpic - 修改头像
```

### Q5: 如何删除 Bot

向 `@BotFather` 发送：

```
/deletebot
```

然后按提示操作。

---

## 安全设置 🔒

### 推荐的安全配置

```json
{
  "channels": {
    "telegram": {
      "enabled": true,
      "botToken": "${TELEGRAM_BOT_TOKEN}",
      "dmPolicy": "pairing",
      "allowFrom": ["你的用户ID"],
      "groupPolicy": "allowlist",
      "groups": {
        "*": {
          "requireMention": true
        }
      }
    }
  }
}
```

### 安全建议

| 建议 | 说明 |
|------|------|
| 🔐 使用环境变量 | 将 `botToken` 放在环境变量中，不要硬编码 |
| 🚫 不公开 Token | Token 泄露会导致 Bot 被他人控制 |
| 👤 限制 allowFrom | 只允许信任的用户访问 |
| 📝 定期检查日志 | 发现异常访问及时处理 |

### 使用环境变量

```bash
# 设置环境变量
export TELEGRAM_BOT_TOKEN="123456789:ABC..."

# 在 openclaw.json 中引用
{
  "channels": {
    "telegram": {
      "botToken": "${TELEGRAM_BOT_TOKEN}"
    }
  }
}
```

---

## 📊 总结

通过本教程，你已经：

- ✅ 创建了 Telegram Bot
- ✅ 配置了 OpenClaw 接入 Telegram
- ✅ 完成了用户配对和授权
- ✅ 测试了消息收发
- ✅ 了解了群组配置

### 下一步

1. **配置 AI 模型**：接入 OpenRouter 免费模型（见 [OpenRouter 教程](./openrouter-step35-free.md)）
2. **设置多用户**：为家人/朋友添加访问权限
3. **创建群组**：在群组中共享 AI 助手
4. **自定义命令**：添加自己的技能命令

---

## 🔗 相关链接

- [Telegram BotFather](https://t.me/BotFather)
- [OpenClaw 官方文档](https://openclaw.dev)
- [Telegram Bot API](https://core.telegram.org/bots/api)
- [OpenRouter 免费模型教程](./openrouter-step35-free.md)

---

**最后更新：** 2026-02-20

**作者：** jqlong17

**许可：** MIT License
